{% extends 'index.html.jinja' %}


{% block main %}
<div class="container mt-5 pt-5">
    <section class="col col-lg-6 justify-content-center mx-auto text-center">
        <h1 class="fs-3 fw-bold">로그인</h1>
        <div class="pyduck-para pt-3 text-secondary">
            더 나은 사이트가 되도록 노력하겠습니다.
            <br>다시 방문해 주셔서 감사합니다.
        </div>
        <form class="pt-5 w-75 mx-auto" action="{{ url_for('auth.sign_in') }}" method="POST">
            <input type="hidden" name="csrf_token" value="{{ csrf_token() }}">
            <div class="form-floating mb-3">
                <input type="email" class="form-control" id="username" placeholder="name@example.com" required>
                <label for="username">이메일 주소</label>
            </div>
            <div class="form-floating">
                <input type="password" class="form-control" id="password" placeholder="Password" minlength="8"
                       maxlength="20" required>
                <label for="password">비밀번호</label>
                <div class="pt-1 text-start">
                    <small class="text-secondary">혹시 까먹으셨나요..?
                        <a href="{{ url_for('auth.forgot_password') }}">비밀번호 초기화하기</a>
                    </small>
                </div>
            </div>
            <div class="pt-4">
                <button class="btn btn-dark form-control">
                    <span>로그인 하기</span>
                </button>
            </div>
        </form>
        <div class="pt-5 text-secondary">
            아직 회원이 아니신가요? <a class="link" href="{{ url_for('auth.sign_up') }}">회원가입 하기</a>
        </div>
    </section>
</div>
{% endblock main %}
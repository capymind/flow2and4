<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container">
        <a class="navbar-brand d-flex align-items-center" href="{{ url_for('csduck.index') }}">
            <div class="pt-1"><i class="bi bi-layers-half"></i></div>
            <div class="ps-1 fw-bold"><span>csduck</span></div>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav nav-underline mx-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="#">Waves</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">Splashes</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">Quacks</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#"></a>
                </li>
            </ul>
            {% if current_user.is_authenticated %}
            {% include "auth/includes/avatar_dropdown.html.jinja" %}
            {% else %}
            <div>
                <a class="btn btn-sm btn-outline-dark rounded-0" href="{{ url_for('auth.sign_in') }}">로그인</a>
                <a class="btn btn-sm btn-outline-secondary rounded-0" href="{{ url_for('auth.sign_up') }}">회원가입</a>
            </div>
            {% endif %}
        </div>
    </div>
</nav>